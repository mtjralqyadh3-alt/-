# نتائج البحث الخارجي

## VirusTotal

المصدر الرسمي: https://docs.virustotal.com/reference/overview
توثيق VirusTotal API v3 يذكر أن نقطة `GET /files/{id}` تستقبل MD5 أو SHA-1 أو SHA-256 مع مفتاح API وتعيد تقرير الملف، وأن API v3 هو الإصدار الموصى به. يذكر التوثيق أيضاً نقطة رفع الملفات للتحليل عبر عدة محركات، لكن الرفع يرسل محتوى الملف إلى خدمة خارجية ويجب أن يسبقه موافقة صريحة وشرح الخصوصية.

المصدر الرسمي: https://docs.virustotal.com/reference/file-info
نقطة التقرير هي `https://www.virustotal.com/api/v3/files/{hash}` مع ترويسة `x-apikey`. يمكن للتطبيق الاستعلام عن ملف معروف بالهاش دون رفعه، لكن عدم وجود تقرير لا يعني أن الملف آمن.

المصدر الرسمي: https://docs.virustotal.com/reference/files
معرّف كائن الملف في VirusTotal هو SHA-256، وتتوفر إحصاءات التحليل مثل malicious وsuspicious وharmless وundetected ضمن `last_analysis_stats`.

## ClamAV

المصدر الرسمي: https://docs.clamav.net/
ClamAV هو toolkit مفتوح المصدر مرخص GPLv2، ويحتوي محركاً كمكتبة مشتركة وأدوات clamscan/clamd، لكنه ليس حزمة حماية Endpoint تقليدية. التوثيق الرسمي يذكر أن المنصات المختبرة هي أنظمة سطح مكتب/خوادم مثل Linux وWindows وmacOS، ولا يقدم Android كمنصة مختبرة رسمياً. لذلك دمجه داخل APK يتطلب بناء Native مخصصاً وقواعد توقيعات وتحديثات، أو خدمة ClamAV خارجية يملكها المستخدم، ولا يجوز الادعاء بوجوده في APK بدون تضمين وتشغيل فعلي.

## قرار معماري

سيُضاف VirusTotal كخيار صريح: اختيار ملف، عرض تحذير مشاركة البيانات، طلب موافقة Android/داخل التطبيق، حساب SHA-256، ثم محاولة التقرير بالهاش أولاً. رفع الملف سيكون اختيارياً منفصلاً وبموافقة ثانية واضحة.

لن يُدمج ClamAV بشكل وهمي. المساران الصادقان هما: (أ) موصل إلى خادم ClamAV يضبطه المستخدم مع TLS وموافقة قبل الإرسال، أو (ب) Native build حقيقي داخل APK مع قواعد توقيعات وتحديثات، وهو أكبر حجماً وأكثر تعقيداً. لا توجد خدمة ClamAV خارجية أو مفتاح VirusTotal متاح حالياً في البيئة لتفعيل فحص سحابي فعلي من جهاز المستخدم.
